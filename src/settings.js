
let settings = {
	canvasWidth: 640,
	canvasHeight: 640,
	bgColor: 128,

	blockSize: 32,

	playerImgInterval: 75,
	playerSpdMult: 2,

	pointSize: 8,

	ghostSize: 24,
	ghostTrackChance: 0.75
};
settings.playerSize = settings.blockSize - 2;
