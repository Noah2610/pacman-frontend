
let settings = {
	canvasWidth: 640,
	canvasHeight: 640,
	bgColor: 128,

	blockSize: 32,

	playerImgInterval: 75,
	playerSpdMult: 2,

	pointSize: 8,
	pointScoreInrc: 10,

	ghostSize: 24,
	ghostTrackChance: 0.5,
	ghostScoreIncr: 100
};
settings.playerSize = settings.blockSize - 2;
